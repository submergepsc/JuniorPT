# pt18 题解概览

## Calculator.cpp
- **题目背景**：使用“数栈 + 运算符栈”计算只含 `+ - * /` 的算术表达式，表达式以 `=` 结束并遵循乘除优先于加减。
- **实现要点**：遍历字符串时遇到数字聚合成多位整数压入数栈，遇到运算符时依据优先级决定是否先弹栈计算。最终在遇到 `=` 或遍历完毕后依次结算剩余运算符，支持除零和非法符号的异常提示。
- **复杂度**：单次扫描表达式，栈操作均摊 O(1)，整体时间 O(n)，空间随运算符与数字数量线性增长。
