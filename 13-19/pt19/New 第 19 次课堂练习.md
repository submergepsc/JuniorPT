# pt19 题解概览

## zzss.cpp
- **题目背景**：维护一批整数的众数（出现次数最多的数），支持初始化、插入新整数并输出当前计数与众数。
- **实现要点**：使用 `std::map<int,int>` 统计每个整数出现次数，构造函数据此确定初始众数；`Insert` 更新计数后按需刷新众数；`Print` 遍历键值对输出 `(值, 次数)` 列表并给出当前众数。
- **复杂度**：插入与统计更新为 O(log n)，打印与重新计算众数（如需）与不同整数个数线性相关。
